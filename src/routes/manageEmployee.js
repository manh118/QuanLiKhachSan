const express = require('express');
const router = express.Router();
const manageEmployeeController = require('../app/controllers/ManageEmployeeController');

// Các route quản lý
// router.get('/show_create', manageEmployeeController.showServiceRoom);
// router.post('/create',uploadService.single('img'), manageEmployeeController.CreateService);
// router.get('/:id/show_update', manageEmployeeController.showUpdate);
// router.put('/:id/update', manageEmployeeController.update);
// router.delete('/:id/delete', manageEmployeeController.delete)


module.exports = router;
